# Dependencias do worker (inclui o motor, engine/) -- roda o processamento
# de verdade (download, transcricao, traducao, dublagem, render). Nao inclui
# FastAPI: o worker nao serve HTTP, so consome jobs da fila no Redis.
python-dotenv
redis
faster-whisper
pyannote.audio
# Separacao de fontes (engine/steps/separate.py): tira a voz original da
# trilha pra sobrar so a musica sob a dublagem. Puxa einops/julius/lameenc;
# torch/torchaudio ja vem do pyannote.audio.
demucs
soundfile
requests
yt-dlp
yt-dlp-ejs
pydantic
